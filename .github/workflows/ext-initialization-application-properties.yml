name: Initialization
on:
  workflow_call:
    inputs:
      environment:
        required: false
        type: string
        default: ''
    outputs:
      destroy:
        value: ${{ jobs.init.outputs.destroy }}
      region:
        value: ${{ jobs.init.outputs.region }}
permissions: write-all
jobs:
  init:
    name: Initialization
    runs-on: ubuntu-latest
    outputs:
      destroy: ${{ fromJSON(steps.destroy.outputs.output).destroy }}
      region: ${{ inputs.environment != '' && fromJSON(steps.settings.outputs.output).settings.region }}
    steps:
      - name: Check out code
        uses: actions/checkout@v3
      - name: Load destruction settings
        id: destroy
        uses: fabasoad/data-format-converter-action@main
        with:
          input: destroy.yml
          from: yaml
          to: json
      - name: teste
        run: echo "${{ fromJson(steps.destroy.outputs.output) }}";
      - name: Load environment application settings
        if: ${{ inputs.environment != '' }}
        id: settings
        uses: fabasoad/data-format-converter-action@main
        with:
          input: ./infra/properties/${{ inputs.environment }}.yml
          from: yaml
          to: json
